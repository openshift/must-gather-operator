apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: must-gather-pvc
  labels:
    test: non-admin-must-gather-e2e
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi